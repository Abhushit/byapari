html
    head
        title Login
        link(rel="stylesheet", href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css")

    body.container

        h2 Login
        p Please login to start your session

        form(name="loginForm", method="POST")
            label(for="name") Name
            input(type="text", name="name", placeholder="Name").form-control
            label(for="email") Email
            input(type="email", name="email", placeholder="Email").form-control
            label(for="username") Username
            input(type="text", name="username", placeholder="Username").form-control
            label(for="password") Password
            input(type="password", name="passwod", placeholder="Password").form-control
            input(type="submit",value="login").btn.btn-primary.mt-3

        p Already registered? Back to 
            a(href="/auth/login") Login